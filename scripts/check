#!/bin/sh
set -eu

root=$(CDPATH='' cd -- "$(dirname -- "$0")/.." && pwd)
cd "$root"

./check.py verify
shellcheck scripts/audit scripts/check scripts/check-witness scripts/package scripts/release \
    scripts/test-wayland scripts/test-x11
